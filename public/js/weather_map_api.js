(function(){
"use strict";


// ====================================Lat/Lon Button========//

		// $(".button").click(function)(getData){
  // 			var findLat = document.getElementById('boxLat').value;
  // 			var findLon = document.getElementById('boxLon').value;
  // 				findLat = parseFloat(findLat);
  // 				findLon = parseFloat(findLon);

  		// };



 //======================================Map Info===============================//               

       var mapOptions = {
           zoom: 10,
              center: {
              lat: 29.4964645,
              lng: -98.478578
          }

       };

       var  mapElement= new google.maps.Map(document.getElementById("map"), mapOptions);

 
  

               

















})();